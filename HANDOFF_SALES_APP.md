# 销售 App 端原型接续 Handoff

这份 handoff 给新会话使用：请在现有 HTML 原型基础上，继续维护“销售 App 端”部分。不要重建项目，不要解释或展开已经完成的患教端内容。

## 项目入口

- 本地原型目录：`D:\糖糖圈工作\收案线上化\改\患教端交互原型`
- 线上预览：`https://mosyoung0710-ai.github.io/case-intake-online-prototype/`
- GitHub 仓库：`https://github.com/mosyoung0710-ai/case-intake-online-prototype`
- 当前分支：`master`
- 主要文件：
  - `index.html`：页面结构、表单题目、右侧修改点文案
  - `styles.css`：布局和视觉样式
  - `app.js`：页面切换、按钮状态、抽屉/弹窗/表单联动
  - `validate-prototype.mjs`：Playwright 本地验收脚本

## 新会话开始前先做

1. 先读取 `index.html`、`styles.css`、`app.js`、`validate-prototype.mjs`，理解当前结构后再增量修改。
2. 先运行 `git status --short --branch`，确认有没有阿雯手改但未提交的内容。
3. 如果阿雯提供销售 app 截图，截图只作为视觉和字段参考；真正要做什么以阿雯对话里的文字要求为准。
4. 不要主动推 GitHub。只有阿雯明确说“推”“推送”“上传 GitHub”时，才提交并推送。

## 当前销售后台问卷相关状态

左侧导航已有“销售部分问卷”，点击后显示一个右侧抽屉式的销售问卷页面。这个页面用于表达销售端先填写部分问卷，患者后续填写时可继续修改销售已填内容。

当前问卷主要结构：

1. 下单/安装信息：业务经理、用户姓名、下单电话、收件地址、下单时间、安装时间、安装方式、用户渠道、服务类型、泵型号、AAPS 安装设备、手机型号/手表型号、售前备注。
2. 一、基本信息：联系电话、用泵人姓名（昵称）、出生日期、性别、身高、体重、糖尿病分型、确诊时间。
3. 二、治疗方案：当前治疗方案，以及按方案切换展示的笔/泵相关字段。
4. 三、相关信息：糖尿病并发症、皮肤相关异常史、血糖监测方式及其后续字段、用泵/用 AAPS 闭环软件主要想改善的问题。

已有联动：

- AAPS 安装设备选“手机”展示“手机型号”，选“手表”展示“手表型号”。
- 糖尿病并发症选“有”后展示具体疾病选项，但销售端具体选项非必填。
- 皮肤相关异常史选“有”后展示子题，销售端子题非必填。
- 当前治疗方案选“餐前+基础”展示笔相关内容。
- 当前治疗方案选“预混”也展示餐前+基础同一套内容。
- 当前治疗方案选“胰岛素泵”展示泵相关内容。
- 注射次数可选 0 次、1 次、2 次；选 1 次或 2 次时，下面展示对应组数的“请选择注射时间”和“请输入注射剂量（U）”。
- 提交按钮已模拟一号一有效看板校验：先校验下单号码是否关联密集照护中的患者，命中时弹出“温馨提醒”并阻止提交；未命中时，再按下单号码、发起经理和患者姓名关系校验进行中的收案任务。左侧“提交校验场景”可切换四种阻断文案。
- 销售部分问卷已增加内部滚动套子：左侧“新增题目/提交校验场景”和右侧“修改点”固定在浏览器一屏内，问卷长内容在中间区域内竖向滚动；字段注释需保留在套子范围内，并向问卷题目外侧平移展示。

## 当前销售 App 端状态

已在左侧导航新增“销售 App 端”。中间主区域使用手机画布表达糖糖圈 App 内的销售端页面，不占用原有销售后台问卷结构。

当前已覆盖页面：

1. 客户池：表达销售在 App 内查看所有用户；搜索区支持按昵称、ID、电话切换条件并模糊查询，不展示页面内搜索按钮；每个用户卡片展示昵称（ID）、性别、年龄、收案进度、手机号、跟进人，并直接展示“新收案”和“收案管理”。
2. 收案管理：按用户展示历史收案列表，包含收案ID、下单日期、提交时间、泵型号、当前状态，以及每条记录的详情、编辑、删除按钮；当前用待接收、已接收、待用户填写、用户填写中、用户已填写、已收案 6 个样例表达按钮权限。
3. 问卷详情：展示销售填写部分和用户填写部分的详情字段。
4. 编辑收案：静态表达历史收案可编辑。
5. 删除确认：从收案管理列表卡片点击删除后，在 App 手机页面内弹出二次确认窗。
6. 系统通知：孤立 App 页面，从左侧“系统通知”导航进入，用于展示患教确认接收收案后发送给发起销售的系统通知。

当前最小交互：

- “销售 App 端”导航打开手机画布。
- 点击用户卡片上的“收案管理”进入历史收案列表。
- 收案管理左上角返回回到客户池；问卷详情返回收案管理；编辑收案返回收案管理。
- 点击历史收案列表卡片里的“详情”进入问卷详情；问卷详情页不提供编辑、删除入口。
- 点击历史收案列表卡片里的“编辑”进入编辑态，点击“保存”或左上角返回回到历史收案列表。
- 点击历史收案列表卡片里的“删除”打开 App 内二次确认窗；点击“取消”关闭弹窗并停留在收案管理；点击“确认删除”后，该条历史收案记录从列表隐藏；点击页面顶部“重置演示”后恢复。
- 点击用户卡片上的“新收案”跳转到已做好的“销售部分问卷”。
- 点击左侧“系统通知”打开孤立通知页；页面展示通知内容“患教已接收患者【JJ】（ID：56）。”作为模板示例。

本轮只画 App 页面承载，不新增真实业务逻辑、不做完整表单校验、不改已有收案状态流转。

## 修改点写法硬规则

右侧“修改点”只写给开发和需求方看的当前改造点，不写阿雯让你一步步怎么改的过程。

可以写：

- 本次新增或改动的入口、字段、状态、校验、显隐条件。
- 页面里看不出来但开发容易误解的规则。
- 销售端和患者端、后台和 app 端之间的差异。
- 需要开发特别同步的数据影响或跨端影响。

不要写：

- “先复刻页面、后续再补题目”这类过程记录。
- 页面上已经清楚展示的字段/按钮清单。
- 已有规则、历史规则、只是为了让原型可见而补出来的规则。
- “本次不改”且不会影响当前页面理解的内容。
- 对阿雯每次纠偏的复述。

销售部分问卷右侧修改点当前应保持克制，类似：

```text
销售部分问卷
1、销售端新增以下题目：……
2、患者填写时可继续修改销售已填内容。
3、同一题支持按销售端、患者端分别配置必填规则；两端提交时分别按本端规则校验。
4、如果本页中未展示的题目，就算满足原有显隐逻辑，也不在销售部分展示，例如吸烟、饮酒。
```

后续做销售 app 端时，应单独写销售 app 端的修改点，不要把销售后台问卷或患教端修改点混进去。

当前销售 App 端右侧修改点应保持克制，类似：

```text
销售 App 端
1、销售在糖糖圈 App 内可查看所有用户，每个用户卡片直接展示“新收案”和“收案管理”。
2、“新收案”复用已做好的销售部分问卷，填写和提交口径保持不变。
3、“收案管理”按用户展示历史收案列表，列表需保留 CRM 中的收案ID、下单日期、提交时间、泵型号、当前状态、问卷详情等关键信息。
4、历史收案详情常亮；编辑仅待接收、已接收、待用户填写可点；删除仅待接收可点，患教确认接收后处理权限转到患教侧。
```

## 代码维护注意

- 纯文案和题目结构大多在 `index.html`。
- 样式位置主要在 `styles.css`，销售问卷样式集中在 `.sales-*` 附近。
- 页面切换和表单显隐主要在 `app.js`。
- 新增页面时，注意同步左侧导航按钮的 `data-view`、主内容区 section、右侧 note-card 的 `data-note`，以及 `app.js` 里的切换逻辑。
- 不要把技术说明写进页面原型内部；技术/规则说明放右侧修改点或 handoff。
- 不要随手加新控件、新提示、新状态。没有阿雯明确要求的，先按截图复刻。

## 验收方式

改完后至少执行：

```powershell
$OutputEncoding = [Console]::OutputEncoding = [Console]::InputEncoding = [System.Text.UTF8Encoding]::new($false)
$env:PROTOTYPE_ROOT='<prototype-root>'
node --check "$env:PROTOTYPE_ROOT\app.js"
node --check "$env:PROTOTYPE_ROOT\validate-prototype.mjs"
node "$env:PROTOTYPE_ROOT\validate-prototype.mjs"
```

如果新增销售 app 页面，应给 `validate-prototype.mjs` 增加对应截图和基础断言，至少确认：

- 新导航入口能打开。
- 页面不是空白。
- 关键标题、字段、按钮存在。
- 右侧只展示销售 app 端对应修改点。

当前已补充销售 App 端验收，完整脚本输出 `ok: true`。新增截图：

- `validation/10-sales-app-users.png`
- `validation/11-sales-app-case-list.png`
- `validation/12-sales-app-detail.png`
- `validation/13-sales-app-edit.png`
- `validation/14-sales-app-delete.png`
- `validation/15-system-notice.png`

## 推送口径

只有阿雯明确要求推送时再做：

1. `git status --short --branch`
2. 运行语法检查和 Playwright 验收
3. `git add` 本次相关文件
4. `git commit -m "..."`
5. `git push`
6. 等 GitHub Pages 构建完成后，再打开线上链接确认 HTTP 200 和页面正常

推送后回复阿雯：线上链接、commit 短哈希、验证结果。不要长篇复盘。
